import 'package:flutter/material.dart';

class ConsultingPage extends StatelessWidget {
  static const routeName = '/consulting';
  @override
  Widget build(BuildContext context) {
    return const Text("Consulting");
  }
}
